# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1

# Assign the correct service account identity to the backend.
# This allows the Firebase Admin SDK to automatically find credentials
# and permissions when deployed.
serviceAccount: "519950387217-compute@developer.gserviceaccount.com"
